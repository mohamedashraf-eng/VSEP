cmake_minimum_required(VERSION 3.10)

project(vsep)

include(CTest)
include("${CMAKE_PROJECT_DIR}/user.cmake")

# Building BSW
add_subdirectory("${bsw_swc}")

# Build Service Layer
add_subdirectory("${sal}")

# Building Application
add_subdirectory("${app_swc}")
